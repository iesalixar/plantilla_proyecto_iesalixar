"use client"; 
import React from 'react';
import LoginForm from '@/components/loginForm/loginForm';

function LoginPage() {
    return(
        <main>
            <LoginForm/>
        </main>
    );
}

export default LoginPage;